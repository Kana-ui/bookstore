<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Bookstore - Home</title>
    <link rel="stylesheet" href="../assets/css/style.css">
</head>
<body>
<div class="page">
    <div class="page-header">
        <h1>📚 Bookstore</h1>
        <div class="nav-links">
            <span class="text-muted">Logged in as: {{ username|e }}</span>
            <a href="logout.php">Logout</a>
        </div>
    </div>

    <p>
        <a class="btn btn-primary" href="add_book.php">➕ Add New Book</a>
    </p>
    <hr style="margin: 16px 0;">

    <h3 style="margin-bottom:8px;">Search Books (AJAX live search)</h3>
    <div class="search-box">
        <form method="get" action="index.php">
            <label>Title:</label>
            <input type="text" name="title" value="{{ titleFilter|e }}">

            <label>Genre:</label>
            <input type="text" name="genre" value="{{ genreFilter|e }}">

            <label>Year:</label>
            <input type="number" name="year" value="{{ yearFilter > 0 ? yearFilter : '' }}">

            <button class="btn btn-secondary" type="submit">Search</button>
            <a class="btn btn-secondary" href="index.php">Reset</a>
        </form>
    </div>

    {% if books is not empty %}
        <p class="text-muted">Found {{ books|length }} book(s).</p>
        <div class="table-wrapper">
            <table>
                <tr>
                    <th>ID</th>
                    <th>Title</th>
                    <th>Author</th>
                    <th>Genre</th>
                    <th>Year</th>
                    <th>Actions</th>
                </tr>
                <tbody id="books-body">
                {% for b in books %}
                    <tr>
                        <td>{{ b.id|e }}</td>
                        <td>{{ b.title|e }}</td>
                        <td>{{ b.author|e }}</td>
                        <td>{{ b.genre|e }}</td>
                        <td>{{ b.publication_year|e }}</td>
                        <td>
                            <a href="edit_book.php?id={{ b.id|e }}">✏️ Edit</a> |
                            <a href="delete_book.php?id={{ b.id|e }}"
                               onclick="return confirm('Are you sure you want to delete this book?');">
                                🗑 Delete
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    {% else %}
        <p>No books found.</p>
    {% endif %}
</div>

<script src="../assets/js/search.js"></script>
</body>
</html>
